# conda env create --file environment.yml
name: cookiecutter_testing
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.9.7
  - pip:
    - pyhere
    